package umc.study.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PreferFoodCategoryRepository extends JpaRepository<PreferFoodCategoryRepository, Integer> {
}